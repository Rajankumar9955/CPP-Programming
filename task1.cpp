#include<iostream>
using namespace std;
int main(){
    int length, width;
    cout<<"Enter the length: ";
    cin>>length;
    cout<<"Enter the width: ";
    cin>>width;
    int abc = (length*width);
    cout<<"Area of rectugular is: "<<abc;
    return 0;
}